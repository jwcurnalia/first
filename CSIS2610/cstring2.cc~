#include <iostream>

using namespace std;

int main(void) {
  char name[32];
  int score;

  while (true) {
    cin.ignore(80,'\n');
    cin.getline(name,31);

    cin >> score;

    if (score == 0)
      break;

    cout << "Name: " << name << "    score: " << score << endl;
  }

  return 0;
}
