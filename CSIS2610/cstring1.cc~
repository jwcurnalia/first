#include <iostream>

using namespace std;

int main(void) {
  char str[32];

  cout << "Enter your name: ";
  cin >> str;

  cout << "Hello, " << str << endl;

  for (int i=0;str[i]!=0;i++)
    cout << (int)(str[i]) << endl;

  return 0;
}
